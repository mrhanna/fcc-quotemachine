import React from 'react';
import ReactDOM from 'react-dom';
import QuoteMachine from './quotemachine/quote-machine.component'

ReactDOM.render(<QuoteMachine />, document.getElementById('quote-machine'));
